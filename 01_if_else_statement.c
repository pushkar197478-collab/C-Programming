#include <stdio.h>

int main() {
     int a = 5;
     if (a > 6)
     {
            printf("a is greater than 6\n");
        }
        else
        {
            printf("a is not greater than 6\n");
     }
    return 0;
}